(function() {
  var creator, overlay;

  overlay = null;

  creator = function(Vue) {
    if (overlay == null) {
      overlay = Vue.extend(creator.obj);
      overlay = new overlay().$mount();
    }
    return overlay;
  };

  creator.obj = require('./overlay-component');

  module.exports = creator;

}).call(this);
